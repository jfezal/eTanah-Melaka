package etanah.view.stripes.penguatkuasaan.disClass;

import etanah.view.stripes.pelupusan.disClass.*;
import etanah.model.KodUOM;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;

/**
 *
 * @author Shazwan 22022012 1112 AM
 * purpose : To Control Pendeposit Kemaskini Icon
 * 
 */
public class DisPendepositController {

    private boolean pendeposit = Boolean.TRUE;
    
    public DisPendepositController viewOnlyPendeposit(){
        DisPendepositController disPControl = new DisPendepositController();
        disPControl.setPendeposit(Boolean.FALSE);
        return disPControl;
    }
    
    public DisPendepositController viewOnlyPendepositPT(){
        DisPendepositController disPControl = new DisPendepositController();
        disPControl.setPendeposit(Boolean.TRUE);
        return disPControl;
    }

    public boolean isPendeposit() {
        return pendeposit;
    }

    public void setPendeposit(boolean pendeposit) {
        this.pendeposit = pendeposit;
    }
    
    
}
