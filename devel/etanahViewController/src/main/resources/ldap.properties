ldap.admin=cn=admin
ldap.pass=etanah123
ldap.login=${ldap.login}
# admin server for hosting eTanah Application
ldap.server=${ldap.server.url}
ldap.port=${ldap.server.port}
# ldap url using cluster address, or using the magic keyword
ldap.url=${ldap.url}

# The OU (organizational unit) to add users to
user.ou=ou=people,ou=myrealm,dc=${ldap.domain}
# The OU (organizational unit) to add groups to
group.ou=ou=groups,ou=myrealm,dc=${ldap.domain}

