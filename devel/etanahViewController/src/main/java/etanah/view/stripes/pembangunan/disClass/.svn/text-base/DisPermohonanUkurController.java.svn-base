package etanah.view.stripes.pembangunan.disClass;

import etanah.model.KodUOM;
import etanah.model.PermohonanBahanBatuan;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;

/**
 *
 * @author Shazwan 13112012 12:11 AM
 * purpose : To Control Permohonan Ukur Kemaskini Icon
 * 
 */
public class DisPermohonanUkurController {

    private boolean tandatangan = Boolean.TRUE;
    private boolean mAsas = Boolean.TRUE;
    private boolean sHakmilik = Boolean.TRUE;
    private boolean bUkur = Boolean.TRUE;
    private boolean checkDMS = Boolean.TRUE;
    
    public DisPermohonanUkurController viewOnlyPU(){
        DisPermohonanUkurController disPUControl = new DisPermohonanUkurController();
        disPUControl.setTandatangan(Boolean.FALSE);
        disPUControl.setbUkur(Boolean.FALSE);
        disPUControl.setmAsas(Boolean.FALSE);
        disPUControl.setsHakmilik(Boolean.FALSE);
        disPUControl.setCheckDMS(Boolean.FALSE);
        return disPUControl;
    }
    
    public DisPermohonanUkurController viewOnlyPUPPT(){
        DisPermohonanUkurController disPUControl = new DisPermohonanUkurController();
        disPUControl.setTandatangan(Boolean.TRUE);
        disPUControl.setbUkur(Boolean.TRUE);
        disPUControl.setmAsas(Boolean.TRUE);
        disPUControl.setsHakmilik(Boolean.TRUE);
        disPUControl.setCheckDMS(Boolean.FALSE);
        return disPUControl;
    }
    
    public DisPermohonanUkurController viewOnlyPUPPTKanan(){
        DisPermohonanUkurController disPUControl = new DisPermohonanUkurController();
        disPUControl.setTandatangan(Boolean.FALSE);
        disPUControl.setbUkur(Boolean.FALSE);
        disPUControl.setmAsas(Boolean.FALSE);
        disPUControl.setsHakmilik(Boolean.FALSE);
        disPUControl.setCheckDMS(Boolean.FALSE);
        return disPUControl;
    }
    
    public DisPermohonanUkurController viewOnlyPUTOJUPEM(){
        DisPermohonanUkurController disPUControl = new DisPermohonanUkurController();
        disPUControl.setTandatangan(Boolean.FALSE);
        disPUControl.setbUkur(Boolean.FALSE);
        disPUControl.setmAsas(Boolean.FALSE);
        disPUControl.setsHakmilik(Boolean.FALSE);
        disPUControl.setCheckDMS(Boolean.TRUE);
        return disPUControl;
    }

    public boolean isbUkur() {
        return bUkur;
    }

    public void setbUkur(boolean bUkur) {
        this.bUkur = bUkur;
    }

    public boolean ismAsas() {
        return mAsas;
    }

    public void setmAsas(boolean mAsas) {
        this.mAsas = mAsas;
    }

    public boolean issHakmilik() {
        return sHakmilik;
    }

    public void setsHakmilik(boolean sHakmilik) {
        this.sHakmilik = sHakmilik;
    }

    public boolean isTandatangan() {
        return tandatangan;
    }

    public void setTandatangan(boolean tandatangan) {
        this.tandatangan = tandatangan;
    }

    public boolean isCheckDMS() {
        return checkDMS;
    }

    public void setCheckDMS(boolean checkDMS) {
        this.checkDMS = checkDMS;
    }
    
    
}
